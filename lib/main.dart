import 'package:flutter/material.dart';

void main() => runApp(
      MaterialApp(
        home: Scaffold(
          body: Center(
            child: IconButton(
              iconSize: 70,
              icon: Icon(Icons.alarm),
              onPressed: () {},
            ),
          ),
        ),
      ),
    );
